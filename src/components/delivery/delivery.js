import { Link } from "react-router-dom";

import {
    Navbar,
    Button,
    ActionIcon,
} from '@mantine/core';

import { Plus } from 'tabler-icons-react';

const Delivery = () => {

    return(
        <div>
            
        </div>
    )
}

export default Delivery;
